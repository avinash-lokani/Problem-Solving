package com.example.app;

import java.util.ArrayList;

public interface ShortestDistance {
     void shortestDistance(ArrayList<ArrayList<Integer>> map, int source, int destination, int noOfVertices);
}
